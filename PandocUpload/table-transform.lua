return {
  {
    Table = function (elem)
        -- if elem.classes == nil then
        --     elem.classes = {}
        -- end
        -- if elem.classes[1] == nil then
        --     elem.classes[1] = "wikitable"
        -- end
        -- return elem
        -- local modified = pandoc.Table(elem.caption, elem.aligns, elem.width, elem.headers, elem.rows);
        -- if modified.classes == nil then
        --     modified.classes = {}
        -- end
        -- if modified.classes[1] == nil then
        --     modified.classes[1] = "wikitable"
        -- end
        -- return modified
    end,
  }
}